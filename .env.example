# Root .env — optional non-secret overrides only.
# Secrets: cp config/demo-credentials.env.example config/demo-credentials.env
# Scripts load credentials via: source scripts/load_env.sh

# Local dev: session login instead of API keys (localhost only; never production)
ERPNEXT_NO_AUTH=1
